const state = {
  showModal: false,
}

const getters = {}

const mutations = {
  // updateCat(state, payload) {
  //   state.foodCategory = [payload]
  // }
}

const actions = {
  // getCat({ state, commit }, payload) {
  //
  // }
}

export default {
  namespaced: true,
  state,
  getters,
  mutations,
  actions
}
